# Resolve peer dependency conflicts automatically
legacy-peer-deps=true

# Prevent automatic peer dependency installation conflicts
auto-install-peers=false

# Show detailed resolution info
loglevel=warn